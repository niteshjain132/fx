<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.cursorws</groupId>
        <artifactId>cursor-ws</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>projecthub</artifactId>
    <name>ProjectHub</name>
    <description>macOS-native engineering project dashboard built with Spring Boot 3 and JavaFX 21</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>net.rgielen</groupId>
            <artifactId>javafx-weaver-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.mkpaz</groupId>
            <artifactId>atlantafx-base</artifactId>
        </dependency>
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-javafx</artifactId>
        </dependency>
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-feather-pack</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.projecthub.ProjectHubApplication</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.projecthub.ProjectHubApplication</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>-Djava.awt.headless=true</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!--
      macOS standalone .app / .dmg via jpackage.
      Activate on a MacBook: mvn -pl projecthub -Pmacos-app package
      Or use the helper script: ./projecthub/scripts/package-macos.sh
    -->
    <profiles>
        <profile>
            <id>macos-app</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.panteleyev</groupId>
                        <artifactId>jpackage-maven-plugin</artifactId>
                        <version>1.6.6</version>
                        <configuration>
                            <name>ProjectHub</name>
                            <appVersion>1.0.0</appVersion>
                            <vendor>cursor-ws</vendor>
                            <destination>target/dist</destination>
                            <input>target</input>
                            <mainJar>projecthub-${project.version}.jar</mainJar>
                            <mainClass>org.springframework.boot.loader.launch.JarLauncher</mainClass>
                            <javaOptions>
                                <option>-Xmx512m</option>
                                <option>--enable-native-access=ALL-UNNAMED</option>
                            </javaOptions>
                            <macPackageName>ProjectHub</macPackageName>
                            <macPackageIdentifier>com.cursorws.projecthub</macPackageIdentifier>
                            <icon>packaging/macos/ProjectHub.png</icon>
                            <type>APP_IMAGE</type>
                        </configuration>
                        <executions>
                            <execution>
                                <id>mac-app-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
